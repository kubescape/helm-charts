{{- $components := fromYaml (include "components" .) }}
{{- if and $components.storage.enabled .Values.storage.mtls.enabled (eq (include "kubescape.certificates.strategy" .) "helm") }}
{{- $certData := include "storage.certgen.certificateData" . | fromJson }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "storage.certgen.secretName" . }}
  namespace: {{ .Values.ksNamespace }}
  annotations:
    {{- include "kubescape-operator.annotations" (dict "Values" .Values) | nindent 4 }}
  labels:
    {{- include "kubescape-operator.labels" (dict "Chart" .Chart "Release" .Release "Values" .Values "app" .Values.storage.name "tier" .Values.global.namespaceTier) | nindent 4 }}
type: kubernetes.io/tls
data:
  ca.crt: {{ $certData.caCrt | b64enc }}
  tls.crt: {{ $certData.tlsCrt | b64enc }}
  tls.key: {{ $certData.tlsKey | b64enc }}
{{- end }}
