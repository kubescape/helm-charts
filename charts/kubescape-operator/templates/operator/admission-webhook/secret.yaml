{{- $components := fromYaml (include "components" .) }}
{{- if and $components.operator.enabled (eq .Values.capabilities.admissionController "enable") (eq (include "kubescape.certificates.strategy" .) "helm") }}
{{- $certData := include "kubescape-admission.certificateData" . | fromJson }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kubescape-admission.secretName" . }}
  namespace: {{ .Values.ksNamespace }}
  annotations:
    {{- include "kubescape-operator.annotations" (dict "Values" .Values) | nindent 4 }}
  labels:
    {{- include "kubescape-operator.labels" (dict "Chart" .Chart "Release" .Release "Values" .Values "app" (include "kubescape-admission.name" .) "tier" .Values.global.namespaceTier) | nindent 4 }}
type: kubernetes.io/tls
data:
  ca.crt: {{ $certData.caCrt | b64enc }}
  tls.crt: {{ $certData.tlsCrt | b64enc }}
  tls.key: {{ $certData.tlsKey | b64enc }}
{{- end }}
